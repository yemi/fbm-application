{
  "name": "application",
  "version": "0.0.0",
  "private": true,
  "author": "Fabian Rios",
  "license": "MIT",
  "dependencies": {
    "@cycle/core": "^4.0.x",
    "@cycle/dom": "^6.0.x",
    "@cycle/history": "^1.1.0",
    "@cycle/http": "^5.0.x",
    "ramda": "^0.17.1",
    "switch-path": "^1.1.3"
  },
  "devDependencies": {
    "babel": "5.6.x",
    "babelify": "6.1.x",
    "browserify": "11.0.1",
    "mkdirp": "0.5.x",
    "npm-watch": "0.0.1"
  },
  "watch": {
    "browserify": "app/**/*.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebrowserify": "mkdirp dist",
    "browserify": "browserify app/app.js -t babelify --outfile dist/main.js",
    "start": "npm install && npm run browserify && echo 'OPEN index.html IN YOUR BROWSER'",
    "watch": "npm-watch"
  }
}
